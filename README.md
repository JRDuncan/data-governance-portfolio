# AdventureWorks Data Governance Portfolio (Ubuntu Build)
```bash
docker compose build --no-cache
docker compose up -d
```
Visit Flask API: http://localhost:8000
Restore AdventureWorksDW2022 manually using SQLCMD.
