name: sqlserver_oltp
serviceType: Mssql  # ← Change from MSSQL to SqlServer
description: OLTP SQL Server for AdventureWorks
connection:
  config:
    hostPort: sqlserver:1433
    database: AdventureWorks2022
    username: sa
    password: "${SQLSERVER_PASSWORD}"
    encrypt: false
